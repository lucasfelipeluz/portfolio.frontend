import { Box } from '@mui/material';
import View from 'src/views/admin/View';

function Register() {
  return (
    <Box sx={{ minHeight: '100vh' }}>
      <View />
    </Box>
  );
}

export default Register;
